version: "3"

services:
  {{name}}:
    container_name: {{name}}
    image: cwryu6252/ml-system-design-pattern:{{name}}_0.0.1
    restart: always
    environment:
      - PLATFORM=docker_compose
    ports:
      - "8000:8000"
    command: ./run.sh